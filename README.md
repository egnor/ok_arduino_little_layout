# OK Little Layout for Arduino

This Arduino library lays out lines of text for small displays using [the u8g2 display driver library](https://github.com/olikraus/u8g2/wiki). Inline markup controls font size, boldness, inverse video, and column layout. Incremental refresh happens for updates.
